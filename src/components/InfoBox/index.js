import React from 'react';
import withModal from '../withModal';

class InfoBox extends React.Component {
    render() {
        return (
            <div>
                info info info
            </div>
        );
    }
}

export default withModal(InfoBox);